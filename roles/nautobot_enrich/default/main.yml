---
# Input: pass in from the previous role: hop_path_to_json output
# Expected format: [{"hop": 1, "devices": [{"ip": "x.x.x.x"}, ...]}, ...]
netpath_hops: []

# Nautobot connection (static for now, recommend Vault later)
nautobot_url: "http://192.168.100.10:8085/api"
nautobot_token: "0123456789abcdef0123456789abcdef01234567"
nautobot_namespace: "Global"
nautobot_validate_certs: false

# Depth helps return related objects (interfaces, device) in one call
nautobot_depth: 2

# Where to save enriched JSON on the controller
nautobot_enrich_artifacts_dir: "./artifacts"
